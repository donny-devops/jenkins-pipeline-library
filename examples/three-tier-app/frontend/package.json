{
  "name": "frontend-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && echo '<h1>Frontend Web</h1>' > dist/index.html",
    "test": "echo 'No tests specified' && exit 0"
  }
}
